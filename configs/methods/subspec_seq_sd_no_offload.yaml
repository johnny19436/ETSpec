# ETSpec
method: subspec_seq_sd_no_offload

llm_path: meta-llama/Llama-3.1-8B-Instruct

recipe:
  class_path: specdecodes.helpers.recipes.subspec.hqq_4bit_no_offload:Recipe

max_length: 4096
cache_implementation: static
warmup_iter: 1
compile_mode: max-autotune

generator_profiling: true

generator_kwargs:
  prefill_chunk_size: 256
  verify_method: exact
  verify_kwargs: {}

# Draft tree parameters
# (These match the old preset defaults)
draft_params:
  temperature: 0.2
  max_depth: 16
  topk_len: 1
